coe:
  application:
    customer-service: customer-service

feign:
  client:
    config:
      default:
        connectTimeout: 600000
        readTimeout: 600000

stubrunner:
  ids: "org.coe.msa:customer-service:+:stubs:6060"
  work-offline: true
